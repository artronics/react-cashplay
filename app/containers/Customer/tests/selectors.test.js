// import { fromJS } from 'immutable';
// import { makeSelectCustomerDomain } from '../selectors';

// const selector = makeSelectCustomerDomain();

describe('makeSelectCustomerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
