// import { fromJS } from 'immutable';
// import { makeSelectCashplayDomain } from '../selectors';

// const selector = makeSelectCashplayDomain();

describe('makeSelectCashplayDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
