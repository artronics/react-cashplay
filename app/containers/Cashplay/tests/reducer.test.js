// import { fromJS } from 'immutable';
// import cashplayReducer from '../reducer';

describe('cashplayReducer', () => {
  it('returns the initial state', () => {
    expect(true).toEqual(true);
  });
});
